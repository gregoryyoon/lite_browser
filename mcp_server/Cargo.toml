[package]
name = "lite_browser_mcp"
version = "0.1.0"
edition = "2021"
description = "Lite Browser AI Agent Automation MCP Server"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1.0", features = ["full"] }
